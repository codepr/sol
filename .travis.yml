os: linux
language: c
compiler:
  - gcc
  - clang

dist:
  - bionic

sudo: true

before_script:
  - sudo apt-get update -qq
  - sudo apt-get install -y libssl-dev python-all python3-all uuid-dev
  - sudo pip install paho-mqtt
  - cmake .

script: make
